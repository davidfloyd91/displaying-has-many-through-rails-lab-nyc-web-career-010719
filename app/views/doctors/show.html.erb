<h1><%= @doctor.name %></h1>

<ul>
  <% @doctor.appointments.each do |appt| %>
    <li><%= link_to appt.patient_name, patient_path(appt.patient) %></li>
    <li><%= appt.date_time %></li>
  <% end %>
</ul>
